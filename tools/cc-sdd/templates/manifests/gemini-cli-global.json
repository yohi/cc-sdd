{
    "version": 1,
    "artifacts": [
        {
            "id": "commands",
            "source": {
                "type": "templateDir",
                "fromDir": "templates/agents/{{AGENT}}/commands",
                "toDir": "{{AGENT_COMMANDS_DIR}}"
            },
            "when": {
                "agent": "gemini-cli"
            }
        }
    ]
}